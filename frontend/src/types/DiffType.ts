/**
 * DiffType 类型定义位置（与 constants/DiffType.ts 的运行时常量重复定义）。
 * ADDED 新增 / REMOVED 移除 / MODIFIED 改写 / MOVED 仅编号或位置移动 / UNCHANGED 未变化
 */
export type DiffType = "ADDED" | "REMOVED" | "MODIFIED" | "MOVED" | "UNCHANGED";
